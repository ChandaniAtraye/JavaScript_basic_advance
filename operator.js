// Arithmetic,comparison,logical ,assignment,unary,ternary

// 1.Arithmetic - used for mathemcatical operations
// + ,- , * , / , % , **


// 2. Comaprison - use to compare two values 
    // == not strict comparison (it check only value)
      // 12 == 13       o/p -false
      // 12 == "12"     o/p - true
    // === Strict comparison (it check both value and type of value)
      // 12 === 13       o/p -false
      // 12 === "12"     o/p - false
    // != not strict comparison
      // 12 != 13       o/p -true
      // 12 != "12"     o/p - false
    // !== strict comparison
      // 12 !== 13       o/p -true
      // 12 !== "12"     o/p - true
    // >= 
    // <=
    // <
    // >  

// 3. assignment - to assign value 
    // = ,+= ,-=, *=, /=, %=
    // let a = 12 
    //  a+=3; // a=a+3 
    //  a-=11;
    //  a*=2;
    //  a/=2;
    //  a%=3;    


// 4. logical 
    // && , || , !  
    
// 5. Unary 
   // +,-,typeOf ,++,--    
     // +"5"
     // -5
     //!12
     // typeOf 12 -- will tell the type 
    //  let a = 3;
    //  ++a;  //4  preincrement - pehle increment karo 
    //  a++;  //3  postincrement - badme 
    // --a ; // 2 predecrement -- pehle decrement karo
    // a--; // 3 postdecrement -- badme decrement karo


// 6 . ternary operator
   // ?:
   //  12>13 ? console.log("true") : console.log("False")  // o/p false 





     